package service

const (
	msgUserIDRequired      = "user_id is required"
	msgTimestampRequired   = "timestamp is required"
	msgDateCannotBeFuture  = "date cannot be in the future"
	msgMonthCannotBeFuture = "month cannot be in the future"
)
